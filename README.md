# Messenger Flutter App

Flutter приложение мессенджера с real-time сообщениями через WebSocket.

## 🚀 Возможности

- ✅ Регистрация и аутентификация пользователей (JWT)
- ✅ Создание приватных чатов
- ✅ Отправка текстовых сообщений
- ✅ Real-time сообщения через WebSocket (Socket.io)
- ✅ Поиск пользователей
- ✅ Статусы пользователей (online/offline)
- ✅ Индикатор печати (typing indicator)
- ✅ Отметка сообщений как прочитанных
- ✅ Список чатов с последними сообщениями
- ✅ Красивый и современный UI

## 📋 Требования

- Flutter SDK (3.0.0 или выше)
- Dart SDK (3.0.0 или выше)
- Backend сервер (см. [Backend README](../us/README.md))

## 🛠️ Установка

1. **Убедитесь, что у вас установлен Flutter:**

```bash
flutter --version
```

2. **Установите зависимости:**

```bash
flutter pub get
```

3. **Настройте URL сервера:**

Откройте `lib/utils/app_config.dart` и измените `baseUrl` на адрес вашего backend сервера:

```dart
static const String baseUrl = 'http://your-server-ip:3000';
```

Для Android эмулятора используйте `http://10.0.2.2:3000` (localhost)
Для iOS симулятора используйте `http://localhost:3000`
Для физических устройств используйте IP адрес вашего компьютера в локальной сети

4. **Запустите приложение:**

```bash
flutter run
```

## 📱 Структура проекта

```
lib/
├── models/              # Модели данных
│   ├── user.dart
│   ├── chat.dart
│   └── message.dart
├── services/           # Сервисы для работы с API
│   ├── auth_service.dart
│   ├── api_service.dart
│   └── socket_service.dart
├── providers/          # State management (Provider)
│   ├── auth_provider.dart
│   ├── chat_provider.dart
│   └── message_provider.dart
├── screens/            # Экраны приложения
│   ├── splash_screen.dart
│   ├── auth/
│   │   ├── login_screen.dart
│   │   └── register_screen.dart
│   ├── chats/
│   │   ├── chats_list_screen.dart
│   │   └── chat_screen.dart
│   └── search/
│       └── search_users_screen.dart
├── widgets/            # Переиспользуемые виджеты
│   ├── message_bubble.dart
│   └── typing_indicator.dart
├── utils/             # Утилиты
│   └── app_config.dart
└── main.dart          # Точка входа
```

## 🔧 Настройка

### Изменение URL сервера

Отредактируйте `lib/utils/app_config.dart`:

```dart
class AppConfig {
  static const String baseUrl = 'http://your-server:3000';
  // ...
}
```

### Настройка для разных платформ

**Android:**
- Для эмулятора: `http://10.0.2.2:3000`
- Для физического устройства: `http://YOUR_COMPUTER_IP:3000`

**iOS:**
- Для симулятора: `http://localhost:3000`
- Для физического устройства: `http://YOUR_COMPUTER_IP:3000`

**Web:**
- Используйте полный URL вашего сервера

## 📡 API Интеграция

Приложение использует следующие endpoints:

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/users/search` - Поиск пользователей
- `GET /api/chats` - Список чатов
- `POST /api/chats` - Создание чата
- `GET /api/messages/chat/:chatId` - Сообщения чата
- `POST /api/messages` - Отправка сообщения

## 🔌 WebSocket Events

Приложение подключается к Socket.io серверу и слушает следующие события:

**Отправка:**
- `message:send` - Отправить сообщение
- `messages:read` - Пометить как прочитанные
- `typing:start` - Начало печати
- `typing:stop` - Конец печати
- `chat:join` - Присоединиться к чату

**Получение:**
- `message:new` - Новое сообщение
- `messages:read` - Сообщения прочитаны
- `typing:start` - Пользователь печатает
- `typing:stop` - Пользователь закончил печатать
- `user:status` - Изменение статуса
- `chat:updated` - Обновление чата

## 🎨 Особенности UI

- Material Design 3
- Адаптивный дизайн
- Анимации и переходы
- Индикатор печати
- Аватары пользователей
- Счетчик непрочитанных сообщений
- Форматирование времени сообщений

## 🐛 Отладка

Для просмотра логов используйте:

```bash
flutter run --verbose
```

Или используйте DevTools:

```bash
flutter pub global activate devtools
flutter pub global run devtools
```

## 📝 Зависимости

Основные зависимости:
- `provider` - State management
- `http` - HTTP запросы
- `socket_io_client` - WebSocket клиент
- `shared_preferences` - Локальное хранилище
- `intl` - Форматирование дат
- `cached_network_image` - Кэширование изображений

## 🚀 Сборка для продакшн

**Android:**

```bash
flutter build apk --release
# или
flutter build appbundle --release
```

**iOS:**

```bash
flutter build ios --release
```

**Web:**

```bash
flutter build web --release
```

## 📄 Лицензия

ISC

## 🤝 Вклад

Приветствуются любые улучшения и предложения!

